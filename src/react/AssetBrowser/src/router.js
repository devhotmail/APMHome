import React from 'react'
import { Router, Route } from 'react-router'
import AssetBrowser from '#/containers/AssetBrowser'

// export default ({history}) => (
//   <Router
//     history={history}>
//      <Route path="/" component={MonopolySim} />
//   </Router>
// )

export default () => <AssetBrowser />
